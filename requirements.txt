# NVIDIA GPU high-performance mode (Windows / RTX 3090):
# 1. The requirements below install the official CUDA 13.0 PyTorch wheel and
#    the matching Windows Triton 3.7 runtime on Windows + Python 3.12.
# 2. Install all project dependencies:
#    python -m pip install -r requirements.txt
# 3. Verify CUDA. The output should contain True and the GPU name:
#    python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'CUDA unavailable')"
#
# Other Python versions and operating systems retain the generic torch path.

# Whisper and audio processing
openai-whisper
librosa
torch @ https://download.pytorch.org/whl/cu130/torch-2.12.1%2Bcu130-cp312-cp312-win_amd64.whl ; sys_platform == "win32" and python_version == "3.12"
triton-windows==3.7.1.post27 ; sys_platform == "win32" and python_version == "3.12"
torch ; sys_platform != "win32" or python_version != "3.12"

# Native file drag-and-drop support for the desktop window
tkinterdnd2

# Link-based MP4 downloads in the GUI
yt-dlp[default]

# Optional dialogue detection for B audio pre-filtering
webrtcvad-wheels
